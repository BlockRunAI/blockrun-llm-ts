name: Publish to npm

on:
  release:
    types: [published]

jobs:
  publish:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write

    steps:
      - uses: actions/checkout@v4

      # pnpm, matching package.json's "packageManager" and ci.yml. This job used
      # `npm ci` against a second lockfile nothing else read, so a package.json
      # change that CI accepted could still kill the publish — which is exactly
      # how v3.6.0 died: "lock file's @blockrun/clawrouter@0.12.220 does not
      # satisfy @blockrun/clawrouter@0.12.223". One package manager, one lockfile.
      - uses: pnpm/action-setup@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          registry-url: https://registry.npmjs.org
          cache: pnpm

      - run: pnpm install --frozen-lockfile

      # ── Release gate ──────────────────────────────────────────────────────
      # Everything below this line runs before anything is published, because
      # npm (like PyPI) will not let you replace a published version.
      #
      # 3.13.1 is why this exists. It bumped package.json and src/version.ts but
      # left VERSION on 3.13.0 and never recorded the release in the changelog.
      # The version-consistency suite catches both — but this workflow triggers
      # on a GitHub Release and ran none of it, so the broken tree went to npm
      # and the failure only surfaced on the push-triggered CI run afterwards.
      #
      # This is the 22 leg of ci.yml, not full CI: 20 and 24 still run only on
      # push, so a version-incompatible API is caught there, not here. The
      # brand-numbers check is deliberately NOT a release gate — it compares
      # marketing counts in the docs, and a stale model number is not a reason
      # to block a bugfix or security release.

      # A release tag that disagrees with the package version means the wrong
      # tree is being published. The "already published" check below would stop
      # a duplicate, but only after the release is cut; fail here instead.
      - name: Verify the release tag matches VERSION
        run: |
          tag="${{ github.event.release.tag_name }}"
          declared="v$(tr -d '[:space:]' < VERSION)"
          if [ "$tag" != "$declared" ]; then
            echo "Release tag $tag does not match VERSION ($declared)." >&2
            exit 1
          fi
          echo "Release tag $tag matches VERSION."

      # A Release cut from an old tag would otherwise publish backwards: npm's
      # "already published" check below only asks whether THIS version exists,
      # so v3.13.3 — tagged 2026-08-26, never released, still on the remote —
      # would install cleanly over 3.13.6 and move `latest` back to a build
      # without the Solana blockhash fix or the eco routing fix. Refuse any
      # version that is not strictly ahead of what npm already serves.
      - name: Refuse to publish backwards
        run: |
          set -euo pipefail
          name="$(node -p "require('./package.json').name")"
          declared="$(node -p "require('./package.json').version")"
          current="$(npm view "$name" version 2>/dev/null || echo '0.0.0')"
          highest="$(printf '%s\n%s\n' "$declared" "$current" | sort -V | tail -1)"
          if [ "$declared" = "$current" ]; then
            echo "$declared is already the published version; the idempotency check below will skip the publish."
            exit 0
          fi
          if [ "$highest" != "$declared" ]; then
            echo "Refusing to publish $name@$declared over the published $current." >&2
            echo "This tag is behind npm — publishing it would move 'latest' backwards." >&2
            exit 1
          fi
          echo "$declared is ahead of the published $current."

      - name: Lint
        run: pnpm run lint

      - name: Typecheck
        run: pnpm run typecheck

      # Covers VERSION <-> package.json <-> src/version.ts and the changelog
      # entry, via test/unit/version-consistency.test.ts.
      - name: Test
        run: pnpm run test --run
      # ──────────────────────────────────────────────────────────────────────

      - run: pnpm run build

      # pack/publish stay on npm: they read package.json, not a lockfile, and
      # `npm publish --provenance` is what wires the OIDC attestation.
      - name: Verify package
        run: npm pack --dry-run

      - name: Check if version already published
        id: check
        run: |
          PKG_NAME=$(node -p "require('./package.json').name")
          PKG_VERSION=$(node -p "require('./package.json').version")
          if npm view "${PKG_NAME}@${PKG_VERSION}" version 2>/dev/null; then
            echo "already_published=true" >> "$GITHUB_OUTPUT"
            echo "⚠️ ${PKG_NAME}@${PKG_VERSION} already published, skipping"
          else
            echo "already_published=false" >> "$GITHUB_OUTPUT"
          fi

      - name: Publish to npm
        if: steps.check.outputs.already_published != 'true'
        run: npm publish --provenance --access public
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
